{
  "name": "@link-credit/workflow",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "bunx tsc --noEmit",
    "simulate": "bun run build",
    "deploy": "cre workflow deploy . --target staging-settings",
    "test": "bun test"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.9",
    "@noble/ciphers": "^2.0.0",
    "viem": "^2.46.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
