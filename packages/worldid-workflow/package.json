{
  "name": "@link-credit/worldid-workflow",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "bunx tsc --noEmit",
    "deploy": "cre workflow deploy . --target staging-settings",
    "test": "bun run build"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.9",
    "viem": "^2.46.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
