{
  "name": "@link-credit/contracts",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "forge build",
    "deploy:sepolia": "forge script script/DeployCreditMarket.s.sol --rpc-url sepolia --broadcast --verify --ffi",
    "test": "forge test --ffi"
  }
}
